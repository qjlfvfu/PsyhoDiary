   server {
    listen 80;
    server_name 111.88.254.169;

    location /static/ {
        alias /app/staticfiles/;
    }

    location / {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
